# 题目二现场答辩讲稿

边界提醒：学生“确认识别文字”只确认转写，确认不等于答案正确。

这份讲稿按“学习工作台 → 实验 / 评估”两个视图的顺序写，正常语速约 4 分钟。不要背术语；每说一个结论，就把鼠标指向页面上对应的字段。

## 演示前准备

1. 提前把 DeepSeek 密钥以单层链接放到 `.private/deepseek_api.txt`（链接目录和目标父目录 mode-0700、目标 owner-only）；双击 `打开题目二教学Agent.command`。启动器会校验目标并在本次 launch 物化为 0600 临时文件，默认 ready 后自动打开 Console。
2. 确认页首显示 `DeepSeek` 和 `deepseek-v4-flash`，不要在降级状态下声称正在调用模型。
3. 保留默认的“动态规划的状态与转移”案例，现场更稳定；若老师要求新题，再修改目标。
4. 浏览器缩放保持 100%，默认停在“学习”视图；右侧“学习检查器”保持展开。
5. 若演示图片，提前准备一张不含姓名、学号或学校的高对比度印刷答案图；不要用真实学生作业测试隐私边界。

## 0:00—0:30：先说清题目要解决什么

指向中间的空对话区和左侧三个学习阶段，说：

> 题目要的不是让大模型一次写出整段教案，而是一个能实时互动的教师 Agent。学生每回答一次，系统才做一次判断：先更新学生状态，再选择或切换 Teaching Skill，最后只生成当前下一步。达到目标就结束；没有进展就停止并转给教师。

接着用一句话说清技术方案：

> 我们用 DeepSeek V4 Flash 理解学生回答、提议 Skill 并生成当前动作候选，用确定性控制器守住状态、路由和停止边界。安全话语只有通过最终 Skill、单动作、问题契约和防答案泄露门禁才会展示，否则由本地确定性 materializer 接管；页面会直接显示动作来源。

> 生产页面还打开了 state-first 路由：模型先提候选，控制器按掌握短板、误解、参与度、无进展次数和 Skill 契约确定优先层，模型只在同层作 tie-break。页面忙碌时的“停止生成”只取消当前回合并保留 Session；显式 `/stop` 才结束整个 Session，`transport_cancellation_supported=false` 表示远程请求本身仍可能完成。

> 这一版 live prompt 是 V18（`teaching_agent_assess_route_act_v18_direct_teaching_cache_stable_prefix`）。teach-first 首次学生可见消息直接进入教师来源约束的解释，不输出阶段政策、定义前测、来源证据卡、内部路由或“回复继续”；练习/核验/迁移的最终解仍受门禁。学生明确说不会时，当前题会即时进入教师换表征讲解/示范约束，不等待下一轮历史提交；grounded clarification 只允许绑定教师来源的回答签发记忆回执；action-only repair 只修当前动作且不能覆盖困惑恢复门禁；请求统计按 `completed_committed_turns_only`，纠错链 taxonomy 只做 teacher-owned 标签归一化，且 `transport_cancellation_supported=false`。这些是工程边界，不是把开发集分数说成部署准确率。

> V18 采用 cache-aware 稳定前缀：固定 system 协议与完整 Skill 合同排在动态 turn 前，以利用 DeepSeek 自动上下文缓存。它没有安装或正式集成 DeepSeek-Reasonix；只能说实现了与其相关的稳定前缀原则。`prompt_cache_layout` 哈希不是缓存命中证据，实际计数只能看 provider `usage.prompt_cache_hit_tokens` / `prompt_cache_miss_tokens`，字段缺失时不估算。

> 历史 run19 的 allowed-Skill hit / switch F1 / bounded completion 为 `0.350000/0.631579/0.400000`，冻结 run25 为 `0.450000/0.818182/0.750000`（fingerprint `4a93ba704f20eb7666f8d371f473bb85233b7f132106c26127db7c92a11d3b7b`）；两者均保留为历史基线。修复“明确自解释证据被保守记为 `partial / ambiguous`，且自解释 Skill 已达重复上限后 Socratic 核验被误拒”后，相同 6 case / 20 turn development input/gold 指纹上的最新真实 DeepSeek run38–run42 allowed-Skill hit 为 `1.00/1.00/0.95/0.95/0.95`，switch F1 均为 `0.916667`，bounded completion 为 `0.75/0.70/0.50/0.80/0.70`。安全例外只允许 Socratic 核验，不提升掌握、不解除误解、不读 gold；run40–run42 的唯一 Skill miss 均为 `cross_session_alpha/alpha_t2`，但跨 session 泄漏与四类 fallback 均为 0。答辩只能说“当前五次开发回归的 Skill 命中均超过 90% 目标”，不能说“Agent Accuracy 或部署准确率为 100%”。这是作者构造、未锁箱的 development fixture，不是 held-out、专家锁箱、部署证据或真实学习效果；bounded completion 的波动也必须主动披露。

> 最新 run43–run47 已在同一 v2 development 指纹上复跑：allowed-Skill hit 为 `1.00/1.00/1.00/0.95/1.00`，switch F1 仍为 `0.916667`，bounded completion 为 `0.80/0.70/0.65/0.65/0.70`。run46 的唯一 miss 仍是 `cross_session_alpha/alpha_t2`；所有 t3 的明确自解释回合均进入 self-explanation/Socratic allowlist，跨 session 泄漏和四类 fallback 均为 0。现场仍只能称为 development regression。

本答辩指南以 V18 direct-teaching + cache-stable-prefix + Guide Learning 困惑恢复 + grounded-clarification + state-first 契约为准；代码和审计字段使用 `action_only_repair`、`continuity_recall`、`misconception_tag_canonicalized_from_teacher_taxonomy` 与 `cancel_turn` 的固定名称，便于现场复核。

## 0:30—1:05：01 目标与画像

指向左侧“新建会话”区域：

> 这里输入的是本次教学目标，不是固定脚本。默认目标是让学生能解释动态规划状态、写出转移，并判断新问题是否适合动态规划。右侧四个初始值分别表示前置、概念、过程和迁移的内部状态。它们只用于控制教学流程，不是学生考试分数。

点击三张合成学生画像中的一张，再展开“学生状态与画像”，补充：

> 三张头像都是 AI 合成图，只帮助演示时区分画像，不对应真人，也不参与能力判断。每张卡一次切换水平、四维掌握和偏好；“既往上下文”只是逐行保存的 `background_history`，没有人工伪造的 signal，不会在开局直接改变学情。切换画像后，按钮会明确显示“切换到某画像并开始”；系统先完整构造并校验新 Session，成功后才替换旧 Session。若 DeepSeek 暂时失败，但醒目标记的安全 fallback 仍通过完整校验，新 Session 可以合法建立；若没有合法候选，旧 Session 保留。教师输入的基础字段不会被模型覆盖；学生回答后，系统只追加一条带置信度、证据绑定状态和“待确认”标记的候选画像；没有绑定到学生原话片段时会强制要求人工复核。

点击“开始实时教学”。

## 1:05—1:40：02 第一轮只生成一个动作

指向中间第一条教师消息，再指向右侧当前主 Skill、支持 Skill 和选择依据：

> 现在系统第一条消息已经直接讲当前概念，然后停下来等学生回答；对话里不会出现“本阶段不考前置知识”“来源证据卡”“回复继续”或内部工具卡。主 Skill、支持 Skill 和选择原因只在右侧检查器查看；一个主 Skill 负责本轮教学意图，支持 Skill 只约束提示力度或等待方式，所以组合 Skill 也不会一次输出多个回合。

再指向 `decision origin` 或运行状态：

> 这里要同时看 decision origin、诊断来源和动作来源。`DEEPSEEK ASSESSMENT` 表示模型原判通过契约；`DEEPSEEK + CONTRACT GUARD` 表示模型判断被确定性约束修正；`ACTIVE CONTRACT EXACT MATCH` 和 `TEACHER KNOWLEDGE SPEC EXACT MATCH` 分别表示本问契约或教师知识规格精确命中；`SAFETY FALLBACK SIGNAL` 才是 API 或校验失败后的规则回退。动作来源若是 `DEEPSEEK SAFE GENERATIVE`，说明模型话语通过全部动作门禁；若是 `DETERMINISTIC MATERIALIZER`，说明本地执行器接管。诊断来源和动作来源不能混为一谈。

## 1:40—2:45：连续三轮互动，展示 Skill 动态切换

根据系统当前问题选择意思相符的回答，不必逐字照搬。下面三句可以直接粘贴。

如需先展示图片输入，可把文字框留空，点击“添加答案图片”上传准备好的印刷文字图，再提交：

> 这里不是把照片直接发给 DeepSeek。原图只在本机临时处理；系统可组合 Apple Vision 与原图、灰度增强、二值图上的多条 Tesseract 路由。远程模型只看到长度受限并经过常见直接标识符模式替换的 OCR 文字、状态和置信度。多路线一致可以建立“公式转写可靠”，但 `formula_accuracy_established` 仍是 false；只有精确命中当前问题契约或教师知识规格才可确定性判对。低置信、路线冲突、键入/OCR 冲突或未获佐证的公式仍要求学生确认。这个演示不代表手写、公式 OCR 或判分部署准确率已经建立。

第一条，先给出一个有用的前置信号：

> 我知道递归可以把大问题拆成更小的问题，但我还不知道 dp 数组的每一项应该表示什么。

提交后说：

> 系统没有只看“对或错”，而是把它判断为部分理解，更新置信度、证据和下一关注点，然后重新选下一轮 Skill。右侧的候选画像也新增了一条本轮观察，但它仍标为未确认。

第二条，主动制造一个可定位的误解：

> 我觉得 dp[i] 只需要看 dp[i-1]，因为第 i 级一定是从前一级走过来的。

提交后指向“判断证据”“误解记录”和 Skill 切换标记：

> 这句话包含一个明确错误规则：忽略了可以跨两级到达。系统把误解和原话证据记下来，并优先切换到误解对比纠错或理解检查，而不是继续机械讲原来的内容。这就是动态决策。

第三条，给出纠正后的回答：

> 还要考虑从 i-2 级跨两级到达，所以状态应该同时看 dp[i-1] 和 dp[i-2]，也就是 dp[i]=dp[i-1]+dp[i-2]。

提交后说：

> 新证据支持后，误解可以从 active 变为 resolved，掌握状态和 Goal 进度随之变化，Agent 再决定进入练习、理解检查还是迁移。后续轮次并不是开始时预先写好的。

如果现场模型没有按预期切到某个具体 Skill，不要硬说它错了。先解释页面给出的理由；需要演示人工接管时，在已经有明确误解证据时输入 `/+skill 误解对比纠错`，完成一至两轮后再输入 `/auto`：

> 教师可以持续锁定一个 Skill，直到输入 `/auto` 恢复自动路由；如果本轮信号不适用、纠错没有证据、超过重复上限或发生安全降级，控制器会主动释放，并在页面解释原因。人工选择会留下 manual override 标记，也不会算成 Agent 自动命中。

## 2:45—3:35：03 学生状态和候选画像为什么不是黑箱

先点右侧“学情”，指向“当前理解”和掌握环；再点“证据”查看上下文请求快照：

> 题目要求显式维护四类信息，这里都能看到：当前掌握、活跃或已解决的误解、本轮理解信号、下一步教学重点。环形图只是四项估计的等权平均，不是四块占比。上下文也不是把聊天全部塞给模型，而是分成固定目标、当前计划、近期工作记忆、较早检查点、知识状态和未确认候选记忆；“证据”页能看到实际请求保留了哪些轮次和用了多少预算。

指向“Agent 候选画像（待确认）”：

> 这里是根据有效 DeepSeek 回合形成的最新候选画像，展示回答质量、参与度、下一重点、置信度和是否需要人工复核。每条都是 `candidate_unconfirmed`，最多保留 12 条；低置信度会醒目标记，规则降级不会生成候选，教师原来填写的水平和偏好也不会被覆盖。

指向 Goal 进度：

> Goal 被拆成前置、概念、过程和迁移四个可观察步骤。系统只有看到对应回答证据才推进；这个进度是流程控制，不是学习效果结论。

## 3:35—4:20：04 评估结果怎么讲

点击顶部“实验 / 评估”，先指向在线开发 benchmark：

> 第一层评估检查单轮自由文本诊断和路由。28 个作者构造案例覆盖四个学科和七类回答，最新在线开发运行的信号 Accuracy 是 0.892857，Macro-F1 是 0.875325，允许主 Skill 命中率是 0.750000，切换 F1 是 0.787879。它使用与 live question-contract 共享的 v3 诊断 taxonomy 和语义量表，但 benchmark prompt 不是完整 live Session prompt，所以不能把这组数字说成多轮 Session 质量；它也不是独立锁箱或真实学生准确率。

再指向多轮 benchmark 说明：

> 我们还构造了 20 个多轮 episode，其中有 65 个学生回答回合和 1 次画像替换操作，专门压长期记忆、图片证据、知识纠错、Skill 切换、画像隔离和终止恢复。gold 不会发给模型；但它仍是作者构造、未经专家复核、未锁箱的开发集。当前只展示评估管线和覆盖范围，不编造尚未完成的在线数值，也不把它说成部署准确率。

如果现场演示 Agent Loop 的路由细节：

> 工具 Loop 的选择不是最终答案。它先读取状态并建议 Skill；如果建议只适用于上一种 signal，服务器会用 state-first 规则重新裁决到可执行阶段。之后若只修教师话语，修复不会改动已经确定的 Skill、终止状态或 route/loop 证据。这是可靠性门禁，不是把模型开发集分数包装成教学准确率。

再指向自适应 Agent 与固定单 Skill表：

> 第二层是四条结构化轨迹的机制回归。自适应 Agent 的允许决策匹配率是 0.916667，终止匹配率是 1；固定基线整段只用逐步支架。这里的模拟增益差是 16.9165，只说明状态机按 fixture 工作，不能说学生成绩提高了 16.9 分。

最后指向前测、后测与迁移测：

> 第三层提供真实学习实验需要的记录接口，可以算前后测、迁移测和可选延迟测。页面当前的 0.4 到 0.8 是作者构造演示，只证明接口能算，真实学习效果还需要学生实验。

## 4:20—4:45：05 主动交代边界并收尾

指向评估视图中的证据边界，再打开页首模型/Skill 状态：

> 默认 v2 库有 13 个主 Skill 和 3 个支持 Skill。它使用 neural-v1 的九环节和十三策略本体，但 neural-v1 的证据物化门禁目前没有通过，所以我们把它明确标为 provisional，没有把候选写成已确认的视频共识。当前完成的是可运行、可审计、可回归的教学闭环；下一步是专家双人复核、锁箱跨 session 测试和真实学习者实验。

最后一句：

> 这个项目最核心的贡献，是把第一题的 Teaching Skill 从静态文档变成了能根据学生反馈实时选择、组合、切换并安全终止的执行系统，同时把“已经验证什么”和“还没有验证什么”明确分开。

## 老师常见追问与一句话回答

### 1. 你们到底用了什么 Agent？

> 是 DeepSeek V4 Flash 语义诊断与 Skill 路由层、确定性状态与安全控制层、服务端 Skill 执行层组成的混合 Teaching Agent。模型负责“理解和选方法”，Skill Runtime 负责“按方法生成这一句话”，不是一次普通聊天调用。

### 2. 为什么不让 DeepSeek 控制全部状态和终止？

> 自由文本理解需要模型，但 Skill 白名单、数值边界和停止条件必须可验证，否则模型可能越权、重复或无限对话。

### 3. 这真的不是提前生成好的对话吗？

> 每个 `step` 只接收当前一条学生回答并返回一个下一动作，后续内容在收到反馈前不存在。

### 4. 学生状态是怎么得到的？

> DeepSeek 从当前回答和相关历史提出带置信度与证据的诊断，控制器再按受限规则更新四维状态和误解生命周期，并把有效诊断追加为待确认画像候选。

### 4.1 Agent 会不会擅自修改老师填写的学生画像？

> 不会；教师字段保持原值，Agent 只在独立的 `adaptive_observations` 中追加待确认候选，低置信度必须人工复核，当前也不会跨 session 保存成长期画像。

### 4.2 你们真的做了上下文吗？

> 做了，而且不是简单拼接聊天记录。每轮只有一个受预算约束的六层上下文：目标与教师画像、当前计划、近期相关回合、较早历史检查点与可 replay 的 `teaching_memory`、掌握与误解状态、未确认候选记忆；本轮回答只出现一次，所有关键状态都有来源指针。`teaching_memory` 只保存显式偏好、未解决问题、教师承诺和“第二种”等指代对象，不由模型编写叙事摘要。默认最多保留 10 个相关回合、总计 14000 字符；超长会话会逐层裁剪。

> 对“第二种呢”“回到第 1 轮”“按约定继续”等显式提示，服务端会生成有证据指针的 `continuity_recall`；找到证据才接续，找不到就明确请学生重述。action-only repair 只拿到有界的 continuity constraints，不会另写一份历史。

### 4.3 自由填写的既往上下文会不会伪造学生状态？

> 不会。网页把每一行写入 `background_history`，在请求中标成 `teacher_provided_unlabeled_background`，没有 `signal` 和 `focus_dimension`，并明确设置为不可更新状态；它只提供背景，开局掌握和理解信号仍来自教师填写值与后续真实回答。

### 4.4 页面上的诊断来源分别是什么？

> 原始 `deepseek_v4_flash` 是未发生归一化的模型诊断；`deepseek_v4_flash_constrained_by_deterministic_contract` 是模型诊断经控制器修正；`active_question_contract_exact_match` 是本问短概念精确命中覆盖不一致模型标签；`deterministic_safety_fallback` 是规则回退。只有第一类能称为模型原判，后三类必须按页面名称如实说明。

### 5. 你们的准确率是不是 89.29%？

> 89.29% 只是在 28 个作者构造、post-hoc 开发案例上的七类信号 Accuracy，真实学生和部署准确率还没有建立。

### 6. 为什么 Skill 命中只有 75.00%？

> 这是严格命中作者预先给定的允许集合；结果说明路由有开发价值也有改进空间，下一步要用专家锁箱标签验证。

### 7. neural-v1 不是已经训练了吗，为什么还写 provisional？

> 训练确实完成，但 54 个 OOF 原子都没有通过证据物化门槛，所以本体可用于暂定执行，不能冒充已确认课堂共识。

### 8. Skill 是从第一题来的吗？

> v2 用 neural-v1 的九环节和十三策略本体组织 12 个操作包装，另外 4 个研究补充 Skill 单独标源，没有伪装成视频发现。

### 9. 多模态在哪里？

> 第一题的多模态用于课堂视频证据抽取和 neural-v1 来源建模；题目二另外支持学生答案图片，但采用的是受限证据链：答案原图留在本机，受支持的 macOS 主机优先用 Apple Vision OCR，不可用、失败或无文字时回退到 Tesseract，其他平台在安装 Tesseract 后使用 Tesseract；随后只把有界、模式脱敏的 OCR 文字交给 DeepSeek。它不是把原图发给多模态大模型，也不是摄像头实时识别。Chrome 验收已经覆盖输入框留空的 image-only 回合，但只使用合成的高对比度印刷文字图。

### 9.1 能可靠识别手写稿和数学公式吗？

> 不能这样声称。当前印刷文字链路可以演示，手写、复杂版面和公式的部署准确率没有独立 gold set；OCR 引擎置信度也不是公式正确率。低置信、无文字、路线冲突或未佐证的公式样文本会要求学生确认转写；确认转写不等于答案正确，只有后续精确命中当前问题契约或教师知识规格才可能判定，系统不会猜图。

### 10. 如何证明 Agent 比普通聊天好？

> 当前有固定单 Skill、常量标签和固定诊断 Skill 基线，证明机制和开发集表现更好；要证明真实教学更好仍需锁箱和学生对照实验。

### 11. 系统会不会直接把答案告诉学生？

> 提示词要求支架式单步行动，验证器拦截明显最终答案表达，但还需要外部行为评测证明复杂场景下的泄露率。

### 12. API 断了怎么办？

> 系统会重试并进入明确标记的规则安全降级，也可以关闭降级让本轮直接失败；降级结果不会伪装成 DeepSeek。

### 13. 学生隐私怎么办？

> 页面和会话服务只在本机，远程只发送必要文字并替换常见直接标识符；答案图片只在本机内存和临时目录做 OCR，原图不发 DeepSeek，远程只收到有界 OCR 文字。声明含直接身份的画像会在远程调用前拒绝。但模式脱敏无法证明自然语言姓名、学校、普通学号或 OCR 误识别出的身份信息都被发现，所以 trace 明确保留剩余身份风险，真实使用仍需授权、人工去标识和数据最小化。

### 14. 真实学习效果做了吗？

> 目前完成了标准化前后测、迁移和延迟测接口，随附数据是演示 fixture；真实效果需要合规学生实验和对照组。

### 15. 现在最大的短板是什么？

> 最大短板不是工程闭环，而是外部效度：neural-v1 证据门禁、专家双人路由标注、跨 session 锁箱和真实学习效果都还未完成。

### 16. 为什么说画像切换和重试是 Codex 风格可靠性，而不是复制 Codex？

> 我们只借鉴官方 Codex 固定提交 `15ea598c6e7e0914a7ae8c881ac05dacea2f7902` 的 thread/turn identity、预期轮次核验、陈旧异步结果隔离、追加式事件和 cold-resume 可靠性模式。当前网页、请求协议、教学上下文和状态机是本项目原创实现，没有复制 Codex 的 UI、Shell、Git、沙箱或多 Agent 能力。默认会话仍在内存；只有显式 `--session-store` 才写本机 hash-chained JSONL，并用 started/committed/aborted、runtime policy 和 Skill 子集门禁冷恢复。

### 17. 你们做了浏览器 E2E 吗？

> 做了真实 Chrome E2E，不再只靠静态解析或 HTTP 冒烟。runner 会在内存中生成一张高对比度印刷文字 PNG，以输入框留空的 image-only 方式检查图片上传、本机 OCR、附件与当前回合绑定、正确回答显示和原图未发送；随后再完成画像 A→B 事务切换、掌握状态与 opaque handle 隔离、旧手动 Skill 不继承、刷新恢复、画像表单回填与评估页，并检查 390/768/1440 三种宽度、控制台、页面错误和失败请求。边界也要说清：这是合成印刷文字的 Chrome 工程验收，不代表手写/公式准确率、Firefox/Safari、屏幕阅读器、真实学生部署或学习效果已经验证。

### 18. “停止生成”和 `/stop` 有什么区别？

> 忙碌时按钮调用 `cancel_turn`，只让当前生成回合失效，Session 仍可继续；迟到响应被 commit fence 丢弃，不会追加历史或轮次。DeepSeek HTTP 请求没有传输层取消，可能仍在后台完成并计费。`/stop` 是教师主动终止整个 Session，状态变为 terminal，二者不能混说。
