# Changelog

- Hardened the live evidence contract (2026-08-08): image-only OCR responses now retain the provider's raw `not_observed` label for audit while a trusted local OCR/question-contract or teacher-claim exact match deterministically adjudicates the final signal; unsupported model signal/alignment repairs are recorded explicitly and never become a remote claim. Continuity recall now requires an explicit unfinished-work/next-step marker for compound prior-agreement requests, with only evidence-backed preference categories projected into the bounded guard. The Agent Loop route is advisory when its signal contract is stale: state-first re-adjudication selects an executable stage before fallback, and action-only repair preserves the fixed route/loop audit fields. Malformed loop actions now fail closed without an internal `KeyError`. The live-session schema validates these additions. The latest local full suite is 860 passed test cases and 1,406 subtests (2,266 checks); release acceptance must be regenerated after final source/document changes.
- Hardened correction-chain routing and taxonomy handling (2026-08-08): an active, evidence-bound misconception now keeps priority over generic retrieval/example routes until a verification Skill can close it; the bounded materializer is used during that chain so a helpful model cannot accidentally echo a canonical answer. Teacher-provided misconception catalogs may declare unambiguous aliases, which are normalized to canonical tags before state updates and retained in the audit normalization reasons. This improves semantic continuity without auto-resolving low-confidence or ungrounded answers.
- Added a wheel/release check for the two public Benchmark v2 development fixtures and repaired dashboard recovery telemetry: retry is shown only for a retryable active request, profile replacement/new-session/restore clears stale retry state, and fallback counts distinguish the current turn from the session total. These are engineering fixes; the development benchmark remains author-constructed and does not establish deployment Accuracy or learning effects.
- Added the evidence-weighted `student_state_estimate.v1` working model for task two. It keeps four mastery dimensions, uncertainty, evidence pointers and focus recommendations using a bounded soft-evidence accumulator (`confidence × answer_alignment_reliability`). The displayed `p_mastery` values are explicitly uncalibrated working estimates, not ground truth, Accuracy, or real learning outcomes; ambiguous or human-review evidence is recorded without moving the estimate.
- Added the recoverable Goal→Plan→Execute→Verify→Reflect orchestration layer. Each phase has a SHA-256-bound checkpoint, bounded replanning and uncertainty-aware handoff; public receipts exclude prompts, chain-of-thought, learner text and tool payloads. The receipt is a control/recovery audit, not a claim of Codex/Claude Code parity.
- Added benchmark v2 with six author-constructed development cases and 20 learner turns split across public input, separate gold, and private predictions artifacts. Validation binds predictions to the exact input fingerprint and permits only runtime safety fields; the multidimensional memory/misconception/switching/injection/isolation/outcome metrics are not a single Accuracy, held-out result, deployment accuracy, or real learning effect.
- Added the student-facing `GOAL / PLAN / PROGRESS / CONTROL` command bar and recovery console with `/auto`, `/+skill`, `/stop`, recoverable `cancel_turn`, draft-preserving retry and explicit handoff. Version guards, idempotency keys and commit fencing keep stale profile/turn responses from overwriting current UI state.
- Bundled the public Benchmark v2 development input and separately stored development gold in the exact release wheel, so an installed package can run offline validation without shipping private predictions or learner data.

- Added the production bounded Agent Loop for task two. With the DeepSeek dashboard backend, each teaching turn now runs structured plan → allowlisted local tool → result-return planning before a validated `route_ready`; only then does the final action planner produce the single next teacher action. The six tools are `inspect_student_state`, `inspect_recent_history`, `search_skills`, `select_skills`, `set_next_focus`, and `evaluate_termination`. The loop is capped at six steps, three tool calls per step, two repeated calls, and one model-boundary retry; invalid plans, unknown tools, repeated calls, and exhausted limits become an explicit `deterministic_safety_fallback` and never masquerade as DeepSeek output. Public receipts keep event types, tool names, counts, hashes and bounded transport metadata, not prompts, chain-of-thought, learner text, media or credentials. This is an original Codex-inspired reliability architecture, not a copy or parity claim for Codex or Claude Code.
- Verified the new path against the configured real `deepseek-v4-flash` endpoint (health check HTTP 200) and two local teaching rounds: the first reached `route_ready` in 6 loop steps with 8 tool calls and no fallback; the next reached `route_ready` in 5 steps with 7 tool calls and switched from diagnostic questioning to Socratic understanding checking. Loopback browser acceptance additionally covered profile replacement followed by another learner turn, refresh recovery of the current action and loop summary, and terminal `/stop` recovery. These are engineering/runtime checks, not open-student teaching accuracy, cross-session generalization, deployment accuracy, or learning-effect evidence.
- Fixed a real browser race in the task-two composer: after a student turn becomes active, the UI now re-synchronizes controls so “停止生成” is visible and enabled while the model request is in flight. Added `scripts/run_teacher_agent_cancel_browser_acceptance.py` and regression tests; a local Chrome receipt verifies recoverable `cancel_turn`, commit-fencing of the late response, draft preservation, and a subsequent committed turn without calling DeepSeek.
- Tightened the full action path around continuity evidence: an ordinal cue such as “第二种呢” must remain visible in the accepted, repaired, or deterministic-guard action, while an unresolved recall must explicitly disclose the missing evidence and ask the learner to restate it. These checks are deterministic and fail closed; a missing-history guard records when execution of the selected Skill is deferred instead of pretending that the referent was understood.
- Repaired image-answer evidence binding when a reliable local OCR transcription and the model quote differ only in harmless presentation details such as whitespace, answer prefixes, terminal punctuation, multiplication/division glyphs, or the two sides of a single equality. The binding still returns the original OCR text, never performs fuzzy spelling repair or algebraic simplification, and keeps low-confidence, conflicting, or uncorroborated formula evidence behind student confirmation.
- Made the live teaching loop easier to read by adding a persistent decision strip for the current Skill, strategy switch, and next focus, with a direct jump to the full selection rationale. Real Chrome acceptance now passes image-only grading, transactional profile replacement (including a controlled stale-guard retry), a post-switch learner turn, refresh recovery, cancellation recovery, and 390/768/1440 responsive checks with no unexpected console, page, or request errors.

## 1.2.0 — Unreleased

This version remains a release candidate: the repository has no Git tag or GitHub
Release for 1.2.0 yet. The entries below cover work through 2026-08-07.

The prior local full-suite receipt recorded 822 passed test cases and 1,367 subtests. After the V15 correction-chain, evidence-repair, continuity-schema and Agent Loop hardening additions, the current local suite is 860 passed test cases and 1,406 subtests (2,266 checks); the release acceptance artifact must be regenerated after the final source and documentation changes. GitHub CI remains a separate remote check for the pushed commit, and any later change inside the acceptance verification scope invalidates a local receipt until it is regenerated.

- Added the complete task-two adaptive Teaching Agent. DeepSeek V4 Flash performs one logical diagnosis-routing-action operation per turn, with bounded transport retries permitted, and returns one teaching action; a deterministic controller owns four-dimension state updates, misconception lifecycle, goal progress, primary/support composition, manual `/+skill` and `/auto` control, and success/unable termination. Each primary Skill's `supporting_skill_ids` is a hard support-composition allowlist. Online plans, manual locks and deterministic fallback share machine gates for Skill ID/role, applicable signal, evidence-backed correction, selected high-risk stage/material preconditions, material availability, primary/support repeat limits, the primary support allowlist and support-local eligibility; absence of any executable Skill stops and escalates. Other natural-language execution-contract clauses remain prompt/audit constraints and are not claimed to be fully machine-understood. The loopback/no-store dashboard exposes every decision and an explicit degraded fallback instead of replaying a pre-generated dialogue.
- Hardened the task-two Agent around real session and semantic failure cases. The dashboard keeps up to 16 isolated Teaching Sessions, restores one session from an opaque browser handle, creates profile changes transactionally, rejects admission when every bounded slot is actively locked, and binds every turn to the session, round, current question, context version, profile revision and idempotency key. A profile replacement also binds the old round, question, context version and profile revision while holding that Session lock, so a stale replacement cannot discard a newly committed turn; the UI additionally rejects stale async responses by request epoch, session/profile identity and non-regressing context version. Sessions remain process-local by default. An explicit `--session-store` now enables local append-only JSONL cold resume with a contiguous SHA-256 hash chain, `turn_started`/`turn_committed`/`turn_aborted` lifecycle, interrupted-turn recovery, checkpointed idempotency caches, exact credential-free runtime-policy binding and fail-closed recovery of order-preserving primary-Skill subsets that retain every support Skill. The store contains private learner/session text, state and bounded OCR evidence but never raw images; the hash chain detects tampering and is not encryption or identity access control. Free-text prior context remains teacher-provided unlabeled background, while evidence-linked replayable teaching memory preserves only explicit preferences, unresolved questions, teacher commitments and referents. A shared question/knowledge contract distinguishes correct, partial and merely related answers. Assessment and action provenance now separately distinguish raw/constrained/exact-match/fallback diagnosis and safe model generation versus deterministic materialization. The UI retains three synthetic portraits, mastery charts and accessible responsive controls. Playwright and HTTP runners cover profile replacement recovery, printed-image input, isolation, refresh, form recovery and responsive widths; these are engineering checks, not deployment or learning-effect validation.
- Added bounded learner answer-image evidence to the task-two Agent. PNG/JPEG/WebP media is validated and processed only in local memory and a temporary directory; supported hosts can combine Apple Vision with original, grayscale-autocontrast and high-contrast-binary Tesseract routes. The raw image is never sent to DeepSeek or persisted: only a length-bounded, pattern-redacted OCR envelope enters the remote teaching context. High-confidence, non-conflicting multi-engine or sufficiently independent multi-preprocessing agreement can establish `formula_transcription_established=true`, while `formula_accuracy_established` remains false. Deterministic correctness requires exact alignment with a gradable current question contract or a teacher-owned knowledge-spec canonical claim whose non-empty `knowledge_components` intersects the current action; an unscoped claim is model context only. Multiline OCR is matched as a complete answer (not by treating one line as the whole answer), and a complete match to a canonical claim for a neighbouring knowledge component is explicitly downgraded to `partial / related_but_not_answer`. The editor preserves evidence IDs and accepts new claims as `知识点::标准陈述`, so a pasted sentence without a knowledge-point binding cannot become an answer key. Low-confidence, empty, conflicting, typed/OCR-disagreeing or uncorroborated formula evidence still forces confirmation, cannot raise mastery or resolve misconceptions, and cannot be bypassed by “the photo is obviously correct.” No handwriting, formula-OCR, answer-grading or deployment accuracy is claimed.
- Rebuilt the task-two surface as a student-first, Codex-inspired learning workspace: task/goal navigation on the left, a single-action conversation with a fixed composer in the center, and an auditable state/Skill/context inspector on the right, with evaluation isolated in its own view and mutually exclusive responsive drawers. Closed drawers are `inert` and hidden from accessibility navigation; open drawers use modal semantics, focus containment, Escape/backdrop dismissal and trigger-focus restoration, with desktop resize clearing modal state. The DeepSeek request now uses one bounded layered context (fixed goal/profile, current plan, recent verbatim turns, deterministic older-history counts plus at most six evidence-linked selective teaching checkpoints, evidence-bound knowledge state, and low-weight unconfirmed profile candidates), while the UI explicitly distinguishes the pre-update request snapshot from the post-update learner state. Teaching checkpoints are extracted only from explicit utterances or recorded signals, retain unknown completion/resolution states, and are explicitly non-exhaustive over omitted-turn semantics; they are not a model-written narrative history summary. Active-KC tags keep multi-component retrieval discriminative; legal oversized sessions shrink to a schema-valid minimum; model-output fallback keeps state/evidence provenance consistent; automatic routing enforces applicable-signal and correction-evidence gates; and both session starts and teaching steps are idempotent and fail closed against stale or cross-tab replacement.
- Added a 16-item v2 runtime library derived from the neural-v1 nine-phase/thirteen-strategy ontology, with separately marked research supplements and per-Skill execution contracts. Neural-v1 was trained on 10 lectures with grouped five-fold OOF, but its evidence materialization gate remains failed (0 eligible, 54 excluded); the UI therefore displays PROVISIONAL and does not claim observed classroom consensus.
- Added two complementary evaluation tracks. Four frozen structured trajectories compare the adaptive controller against a fixed single-Skill baseline (decision match 0.916667; simulated gain delta +16.9165), while the latest 28-case author-constructed DeepSeek development benchmark reports Signal Accuracy 0.892857, Macro-F1 0.875325, allowed-Skill hit 0.750000, switch F1 0.787879, termination F1 1.0, zero call failures and P50/P95 wall latency of 994.887/1250.134 ms. The benchmark uses the v3 diagnosis taxonomy and semantic rubric shared with the live question-contract path, but its prompt is a separate single-turn diagnosis/routing prompt rather than the complete live Session prompt. It therefore remains post-hoc same-set single-turn development evidence—not a complete live Session evaluation, expert validation, a held-out lockbox, deployment accuracy, or a real learning effect.
- Added an author-constructed 20-episode adversarial multi-turn benchmark with 65 learner turns plus one profile-replacement operation, covering long-horizon memory, visual evidence, knowledge correction, Skill switching, profile isolation, prompt-injection resistance, termination and recovery. Its runner excludes every gold field from model payloads and pairs a compatibility-named `current` arm running `deterministic_legacy` against the production-default integrated `safe_generative_executor`. Under the V15 state-first live contract, each turn first makes one diagnosis/routing/action-candidate plan request; only when the candidate action does not satisfy the finalized route/action contract, action-only repair is enabled and the bounded eligibility gate passes may the safe arm make at most one fixed-route action-only repair request. That repair cannot change diagnosis, primary/support Skills, termination or route and falls back to the already materialized action if it fails. Transport retries remain part of one request and never create an extra teaching action. Reports distinguish request topology, production `action_provenance`, validated-plan count and end-to-end turn latency. No unrun or unreviewed online score is reported. The fixture is not expert reviewed or held out after development and does not establish full live-Session quality, cross-session generalization, deployment accuracy or learning effects.
- Clarified multi-turn request accounting: `request_accounting_scope=completed_committed_turns_only`; a repair is not a second validated plan but is a second logical model request, so reports expose `validated_plan_request_total`, `action_repair_request_total`, `logical_model_request_total`, per-completed-turn means, and repair adoption rate separately. Failed, aborted, or uncommitted turns are excluded from these request totals and reported through completion/failure counters. Busy “停止生成” uses recoverable `cancel_turn`; `/stop` remains terminal Session stop, while remote transport cancellation is still unsupported and late responses are commit-fenced.
- Recorded the V15 state-first continuity boundary: service-generated `continuity_recall` may only follow evidence-linked excerpts, `action_only_repair_enabled` cannot rewrite history, correction-chain taxonomy/contract normalization cannot bypass evidence gates, and `transport_cancellation_supported=false` remains an explicit deployment caveat.
- Added a self-contained public aggregate evidence dashboard with a multimodal timeline, four-arm TeachObs ablation explorer, DIPSER scope comparison, executable Skill state-machine demo, claim-boundary matrix, responsive layouts, and persistent visual tweaks; `tsm dashboard` opens it without a server or frontend dependency.
- Added dashboard privacy/self-containment checks, source and exact-wheel CLI smoke coverage, and strict wheel allowlisting for the reviewed HTML resource; no private media, transcript text, OCR rows, labels, predictions, or embeddings are embedded.
- Declared Pillow in the development extra because the browser-acceptance unit tests generate an in-memory answer image during normal test collection; clean Linux CI no longer depends on an undeclared workstation package.
- Expanded `tsm dashboard-real` into a two-outcome local defense surface. The TeachObs view retains authorized complete classroom video, captions, ten traceable teacher-visible action projections and frozen four-arm per-sample predictions while explicitly leaving student actions unmodelled. A separate MIT view now loads ten fingerprint-bound Full Skills, preserves each lecture's actual nine-step procedure order, separates `observed_method` from `recommended_enrichment`, shows referenced `evi_*` captions and sanitized selected `mme_*` candidates, parameterizes the active Skill with a presenter-supplied concept to generate a complete teaching process, displays the hash-bound seven-dimension/six-gate automatic evaluation, and runs a local pass/fallback state-machine demonstration. Raw Skill paths, URLs, OCR text and job directories are removed from browser DTOs. The two datasets remain independent validation tracks: TeachObs predictions are not direct inputs to the MIT procedure, whose direct `mme_*` reference count is currently zero. The loopback/no-store/privacy controls remain unchanged; neither view establishes live deployment, expert Skill quality, event accuracy or learning effectiveness.
- Added deterministic cross-lecture general-Skill distillation and application commands. The new wrapper validates every source Skill, gives each lecture one vote, requires both 80% overall and 60% within-course support, excludes all recommended scaffolds from observed consensus, keeps source fingerprints and claim boundaries, emits a nine-stage executable policy for unseen concepts, and evaluates only structural/support readiness rather than Accuracy. The neural multimodal v1 training path is implemented and executed, but its all-uncertain OOF output fails the strict evidence gate and therefore remains provisional.
- Packaged demo data, evaluation cases, schemas, and claim-contract configuration for non-editable wheel installs.
- Added `doctor`, `verify-delivery`, `release-audit`, aggregate DIPSER export, and strict frozen-recognition CLI workflows.
- Added fail-closed DIPSER dataset/feature fingerprint recomputation and tamper tests.
- Generalized strict feature-bundle validation to exact sample order, modality/feature schemas, extractor provenance, and finite float64 matrices, while narrowly containing legacy DIPSER compatibility.
- Upgraded the identity-aware frozen model to checkpoint v3, binding the exact strict training feature bundle, class schema, claim contract fingerprint, and frozen claim-cluster field.
- Added Ed25519 freeze registration, exact external dataset/feature/coverage binding, atomic one-time ledger consumption, signed evaluation receipts, and delivery-side trusted-key verification.
- Added fail-closed coverage denominator accounting and claim-cluster independence gates, including rejection when a participant or teacher spans multiple claimed independent clusters.
- Added `extract-strict-features`, a private raw media/sensor bridge that binds source bytes and windows, sample order, extractor code/configuration, runtime, and FFmpeg/FFprobe provenance without making predictions.
- Added signed, externally governed aggregate evidence manifests for confirmatory multimodal gain and real learner effectiveness, with recomputed study gates and separate out-of-band trust anchors in delivery verification.
- Added Skill procedure provenance, observed/recommended separation, and complete human-review coverage validation.
- Bound every human-review row to the exact canonical Teaching Skill SHA-256 so stale ratings cannot validate modified content under the same Skill ID.
- Tightened formal transcript readiness around source/media hashes, full coverage, OpenAI Whisper model/decoder provenance, independent WER checks, or complete human review records.
- Added CI, isolated wheel smoke testing, privacy governance, atomic artifact writes, and real-media download hardening.
- Added allowlist-only clean release builds with fixed `SOURCE_DATE_EPOCH`, byte-identical double-build verification, and direct verification of the exact wheel intended for distribution.
- Added a fail-closed release finalizer that invalidates stale acceptance first, binds JUnit counts and all receipts to the exact wheel and current verification tooling, audits the wheel/public directory/final acceptance, and never carries old positive fields forward.
- Hardened release and tracked-file audits against renamed media/archives/models, UTF-8/UTF-16 secrets, identity fields hidden in parsed JSON string tokens, unknown binary payloads, unsafe/duplicate/symlink ZIP members, archive resource bombs, and oversized tracked files.
- Bundled privacy, security, third-party-data, and changelog governance documents inside wheel installs.
- Marked scripted state-machine completion in machine-readable artifacts as demonstration-only and explicitly not learner-effectiveness evidence.
- Expanded publication audits and private permissions for media, models, arrays, archives, databases, binary magic signatures, frames, and row-level identity/path fields; passing remains a guard, not a de-identification proof.
- Kept independent human review, learner effects, confirmatory multimodal gain, and deployment accuracy explicitly pending external evidence.
- Added a hash-pinned MIT OCW formal-caption source index and `fetch-formal-captions`, which verifies each official page's WebVTT/media pair, caption content type and SHA-256, live FFprobe duration, and conservative first-to-last-cue coverage before publishing a private 2×5 formal manifest.
- Completed and independently re-audited the 10/10 official-caption retrieval in this workspace while keeping caption text out of the wheel and hash-only public receipt.
- Hardened formal transcript audit against manifest/transcript identity mismatches and duplicate caption URLs or content hashes, and added `audit --require-formal` for fail-closed automation.
- Added private, resumable download and FFprobe validation for the 10 page-linked MIT OCW complete videos, with local SHA-256 commitments and a content-free public receipt.
- Added resumable whole-timeline chunk processing for complete lectures: full-track silence detection, uniform and scene-change frames, Tesseract OCR, pixel/board/slide change evidence, formal-caption/media alignment, and per-lecture multimodal event fusion.
- Corrected Tesseract TSV quoted-field parsing in longform extraction v2, advanced the full pipeline to v9, invalidated the earlier OCR-derived aggregates, and reran all 10 lectures plus semantic attachment and ablation.
- Completed the corrected v9/v2 10-lecture run over 1,038,813,006 bytes / 26,656.83 seconds, producing 2,553 hash-bound frames, 40,191 threshold-accepted OCR words, 1,974 visual events, and 2,270 fused events with 10/10 timeline coverage and caption/media binding.
- Added hash-bound CLIP visual semantics for all 2,553 frames using a fixed eight-class ontology, complete model revision/weight/runtime provenance, and explicit uncalibrated-score/accuracy boundaries.
- Packaged the CLIP implementation behind the optional `visual` extra with `visual-semantic-extract`, `visual-semantic-dataset`, and `visual-semantic-apply` CLI entry points; repository scripts remain compatibility wrappers.
- Added paired transcript-only, transcript+audio, transcript+visual/OCR, and full internal ablations over identical transcript segments; the report explicitly separates internal rubric/evidence consistency from recognition accuracy or causal gain.
- Added aggregate-only public multimodal and ablation receipts that commit to private source artifacts while excluding media, text, frames, embeddings, per-lecture records, identifiers, and paths.
- Added `run_full_video_multimodal_study.sh` to orchestrate the complete resumable 10-video workflow through semantic attachment, four-arm ablation, public receipts, and release audit, with CPU defaults and explicit GPU environment overrides.
- Made TeachObs credentialed YouTube media recovery use cookie-capable `default,web_safari` clients while retaining anonymous `android_vr`, preserving the actual client and local yt-dlp/EJS/runtime provenance across media reuse, and suppressing private browser-profile details from failure and timeout exception chains.
- Advanced the offline TeachObs ASR worker/result contract to v3: VAD-restored overlong wall-clock segments are split on positive-duration word anchors, while finite in-segment zero-duration point words are attached to the temporally nearest adjacent positive word (previous on ties) without becoming boundaries; the runner proves monotonic assignments, enforces the 30.5-second maximum distance, preserves exact text and positive boundaries, and records reconciled counts plus maximum observed distance for fail-closed import validation.
- Advanced the TeachObs ASR job/result/runner contracts to v2/v4/v4 and replaced the arbitrary 30-second VAD endpoint gates with one symmetric, duration-relative policy: hash-bound full-media single-pass input, at least 0.90 first-to-last speech-anchor span, and at most 0.10 media-duration gap at either endpoint. Legacy evidence fails closed and all selected lessons must be rerun under the new manifest; no result migration or failed-only rerun is accepted.
- Moved the ASR wheel-specific image label after the fixed OS/CUDA/Whisper layers so a source-only rebuild still changes the final image digest and receipt without needlessly invalidating multi-gigabyte dependency caches.
- Completed the six selected offline GPU ASR jobs under the v2/v4/v4 contract and re-imported all six with hash-bound media/model/runtime/timeline checks, yielding 29/30 transcript-source coverage (19 creator-provided captions, 4 automatic captions, 6 audited-ASR fallbacks, with only S4 pending) while explicitly leaving WER and content accuracy unestablished.
- Added a fail-closed TeachObs transcript materialization bridge that binds the selected platform-caption/audited-ASR provenance and fixed 4,945-scene paper-profile order into the four-arm benchmark input; the older released-text transcript-only numbers remain a separately named compatibility baseline.
- Advanced the TeachObs materialization manifest/receipt to v2 with deterministic target-window provenance: overlapping platform cues are endpoint-clipped without text loss, zero-intersection platform cues fail closed, and full-media ASR tail segments outside the official scene domain are explicitly counted and excluded while remaining bound to the source media duration.
- Replaced the TeachObs four-arm fixed-threshold classifier with a train-only, five-fold lesson-grouped OOF revision: every fold refits TF-IDF/IDF and numeric scalers, selects from a fixed balanced-logistic C registry, and selects per-label thresholds from a fixed grid using pooled OOF Micro-F1 with deterministic tie-breaks. Frozen model schema v2 binds the selected C, threshold arrays, and selection provenance and still verifies exact safe-NPZ prediction/probability parity. Because the first public-test run was already observed, all private/public artifacts explicitly mark this as a post-test iterative exploratory revision rather than a first blind or confirmatory evaluation.
- Completed the formal 29-lesson / 4,945-scene TeachObs materialization and revised four-arm run. Transcript/audio achieved the best Micro-F1 (0.604708), transcript-only the best Hamming accuracy (0.818833), while full multimodal improved Macro-F1 over transcript-only by 0.065266 with a six-lesson paired-cluster 95% interval of [0.014886, 0.095637] but reduced Micro-F1 and Hamming accuracy. The fixed-threshold first attempt remains preserved privately rather than overwritten.
- Upgraded the TeachObs new-site preregistration to schema 1.2 / protocol v2 so its analysis plan binds the four exact 39-label `model_thresholds` array hashes and the transitive artifact-set fingerprint instead of incorrectly hard-coding a scalar 0.5 threshold.
- Made completed TeachObs ASR/materialization require a current benchmark, paper-profile human-review skeleton, and transitively bound lockbox draft in final verification; the project receipt now records and later recomputes the exact selected human manifest/receipt/lockbox paths and hashes, including fresh-root environment overrides.
- Replaced the fixed six-step procedure template with distillation of the nine canonical teaching phases named in the task statement. Each phase is located by cue matching against the transcript and the procedure is emitted in the teacher's own temporal order rather than a template order, so matched steps carry real timespans, matched cues, and evidence ids under `origin=observed_method` while unmatched phases remain explicitly labelled `recommended_enrichment` scaffolding. The ten demo lectures recover four to seven observed phases each, and `mining_metadata.method` advanced to `interpretable_heuristic_v2_phase_sequence`.
- Added the `method_fidelity` evaluation dimension and reweighted the rubric to seven dimensions (structure 12%, evidence 18%, executability 18%, method fidelity 22%, pedagogical quality 12%, generalizability 9%, traceability 9%). The other six dimensions check fields the miner emits by construction and were saturated at a population standard deviation of 0.000 across all ten demo skills, so they measured packaging rather than distillation. `method_fidelity` instead re-derives each observed step's claims from the evidence records it cites — a step scores only if its claimed cue literally occurs in its own quotes, its claimed timespan actually contains every record it references, and its evidence ids resolve. Demo skills now span 83.3 to 89.8 on the new dimension and 92.3 to 93.7 overall, and a sixth gate `method_distilled_from_video` fails any skill below 40.
- Added negative-control degradation tests that score deep copies of real skills after template-stripping, cue fabrication, timespan shuffling, evidence removal, and phase collapse, asserting both a strictly lower total and that each degradation is caught by the specific sub-component meant to detect it; pinning `method_fidelity` to a constant fails eight of them.
- Extended the shipped demo response script to cover the full nine-phase procedure and added a regression test binding its length to the distilled procedure, fixing a scripted `interact` session that ended incomplete and made `run_multimodal_demo.sh` exit non-zero.
- Recomputed the four-arm ablation and its public aggregate receipt under the revised rubric; the source transcript set commitment is unchanged and the paired-delta structure is preserved, while mean internal scores move from 100.00/100.00/99.92/99.94 to 95.37/95.37/95.30/95.32. These remain internal structural scores rather than recognition accuracy or causal multimodal gain.
- Completed a documentation-staleness sweep, corrected the obsolete pre-`method_fidelity` ablation means and six-dimension summaries that remained in public-facing documentation, separated the read-only verification of the previously pushed HEAD's successful Actions run from the local acceptance proof chain, and added regression tests that bind the numerical claims to current public receipts and evaluator weights.
- Expanded release-acceptance freshness binding to the complete package source, dashboard, schemas/configuration, README/governance files, and research documentation; dashboard privacy flags are now derived from the same fail-closed release audit, with embedded media and row-level identity negative controls.
