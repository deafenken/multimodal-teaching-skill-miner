# Private OUC-CGE artifacts

This directory may contain row-level labels, predictions, feature caches, checkpoints, and information derived from identifiable classroom video. Keep it local and access-controlled. Public releases may contain only aggregate, identity-free summaries that pass the release audit.
